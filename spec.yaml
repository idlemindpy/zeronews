#! spec.yaml

name: zero
region: TOR
services: 
  - name: zero

dockerfile_path: Dockerfile

source_dir: .

gitlab:
  branch: main
  deploy_on_push: true
  repo: idlemind-rust/newsletter.git

health_check:
  http_path: /health-check

http_port: 8000

instance_count: 1
instance_size_slug: basic-xxs

routes:
  - path: /
